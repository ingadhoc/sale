<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="sot_sale_order_type_form_view" model="ir.ui.view">
        <field name="name">sale.order.type.form.view</field>
        <field name="model">sale.order.type</field>
        <field name="inherit_id" ref="sale_order_type.sot_sale_order_type_form_view"/>
        <field name="arch" type="xml">
            <field name="journal_id" position="after">
                <field name="invoice_company_id" invisible="1"/>
            </field>
            <field name="sequence_id" position="after">
                <field name="auto_done_setting" invisible="1"/>
            </field>
               <group position="inside">
                <group string="Automations">
                    <field name="picking_atomation"/>
                    <field name="book_id" attrs="{'invisible' : [('picking_atomation', '=', 'none')]}"/>
                    <field name="invoicing_atomation"/>
                    <field name="payment_atomation" attrs="{'invisible' : [('journal_id', '=', False)]}"/>
                    <div class="o_row o_wrap_label" attrs="{'invisible' : [('payment_atomation', '=', 'none')]}">
                        <label for="payment_journal_id"/>
                        <i class="fa fa-exclamation-circle" data-tooltip="This field will not be taken into account for sales that come from eCommerce, in this case you must configure it directly in Settings" title="This field will not be taken into account for sales that come from eCommerce, in this case you must configure it directly in Settings" aria-hidden="true"/> 
                    </div>
                    <field name="payment_journal_id" nolabel="1" attrs="{'invisible' : [('payment_atomation', '=', 'none')], 'required' : [('payment_atomation', '!=', 'none')]}"/>
                    <field name="set_done_on_confirmation" attrs="{'invisible' : [('auto_done_setting', '=', True)]}"/>
                </group>
            </group>
        </field>
    </record>
</odoo>