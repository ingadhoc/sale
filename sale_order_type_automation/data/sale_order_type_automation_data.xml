<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="action_sale_order_confirm_force_background_post" model="ir.actions.server">
        <field name="name">Confirm anyway (validate invoice in background)</field>
        <field name="model_id" ref="sale.model_sale_order"/>
        <field name="state">code</field>
        <field name="code">action = records.action_confirm_force_background_post()</field>
    </record>

    <record id="action_picking_validate_force_background_post" model="ir.actions.server">
        <field name="name">Validate anyway (validate invoice in background)</field>
        <field name="model_id" ref="stock.model_stock_picking"/>
        <field name="state">code</field>
        <field name="code">action = records.action_validate_force_background_post()</field>
    </record>

</odoo>
