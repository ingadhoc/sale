<?xml version="1.0"?>
<odoo>

    <record model="ir.actions.act_window" id="action_products_portal_tree">
        <field name="name">Products</field>
        <field name="view_mode">tree</field>
        <field name="res_model">product.product</field>
        <field name="view_id" ref="product.product_product_tree_view"/>
        <field name="context">{"portal_products":True}</field>
    </record>
    <menuitem
        id="products"
        name="Products"
        action="action_products_portal_tree"
        parent="sale_menu"
        sequence="30"
        />

    <record id="view_stock_product_tree" model="ir.ui.view">
        <field name="name">product.product.inherit</field>
        <field name="model">product.product</field>
        <field name="inherit_id" ref="stock.view_stock_product_tree"/>
        <field name="groups_id" eval="[(4, ref('group_portal_distributor'))]"/>
        <field name="arch" type="xml">
            <field name="qty_available" position="attributes">
                <attribute name="invisible">1</attribute>
            </field>
            <field name="virtual_available" position="attributes">
                <attribute name="invisible">1</attribute>
            </field>
        </field>
    </record>
    <record id="product_product_tree_view" model="ir.ui.view">
        <field name="name">product.product.inherit</field>
        <field name="model">product.product</field>
        <field name="inherit_id" ref="product.product_product_tree_view"/>
        <field name="groups_id" eval="[(4, ref('group_portal_distributor'))]"/>
        <field name="arch" type="xml">
            <field name="lst_price" position="attributes">
                <attribute name="invisible">1</attribute>
            </field>
        </field>
    </record>
</odoo>
