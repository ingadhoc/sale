<?xml version="1.0"?>
<openerp>
    <data noupdate="0">
    <!-- we add this dummy rules so that user can edit products just to add quantities in sale orders -->

    <record id="product_product_read_only_all" model="ir.rule">
        <field name="name">Product Product Read Only for employees, portal</field>
        <field name="model_id" ref="product.model_product_product" />
        <field name="domain_force">[('name', '=', False)]</field>
        <field name="groups" eval="[(6, False, [ref('base.group_user'), ref('base.group_portal')])]"/>
        <field name="perm_read" eval="False"/>
    </record>

    <record id="product_template_read_only_all" model="ir.rule">
        <field name="name">Product Template Read Only for employees, portal</field>
        <field name="model_id" ref="product.model_product_product" />
        <field name="domain_force">[('name', '=', False)]</field>
        <field name="groups" eval="[(6, False, [ref('base.group_user'), ref('base.group_portal')])]"/>
        <field name="perm_read" eval="False"/>
    </record>

    </data> 
</openerp>

