<?xml version="1.0"?>
<odoo>

    <record id="view_order_l10n_ar_form" model="ir.ui.view">
        <field name="name">sale.order.l10n_ar.inherit</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="portal_sale_distributor.view_order_form"/>
        <field name="priority">40</field>
        <field name="arch" type="xml">
            
            <field name="validity_date" position="after">
                <field name="sale_checkbook_id" groups="portal_sale_distributor.group_portal_backend_distributor" invisible="1"/>
            </field>
            
        </field>
    </record>

</odoo>
